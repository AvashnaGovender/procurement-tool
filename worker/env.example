# =============================================================================
# AI DOCUMENT ANALYSIS WORKER - ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and update with your actual values
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE CONFIGURATION
# -----------------------------------------------------------------------------
# PostgreSQL connection string (shared with main Next.js application)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# Required for storing analysis results
DATABASE_URL=postgresql://postgres:Admin123@localhost:5434/procurement_db

# -----------------------------------------------------------------------------
# OLLAMA CONFIGURATION (AI Processing)
# -----------------------------------------------------------------------------
# Local LLM server for document analysis
# Install: https://ollama.ai/download
# Required for AI-powered document validation

# Ollama server URL (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Model to use for analysis (recommended: llama3.1:latest)
# Run: ollama pull llama3.1:latest
# Options: llama3.1:latest (4.7GB), llama3.1:8b (4.7GB), llama3.1:70b (40GB)
OLLAMA_MODEL=llama3.1:latest

# -----------------------------------------------------------------------------
# WORKER SERVICE CONFIGURATION
# -----------------------------------------------------------------------------
# FastAPI server settings

# Host to bind to (0.0.0.0 for Docker, localhost for local dev)
API_HOST=0.0.0.0

# Port for the worker service (default: 8001)
API_PORT=8001

# Number of concurrent workers (adjust based on CPU cores)
WORKER_CONCURRENCY=4

# -----------------------------------------------------------------------------
# FILE STORAGE
# -----------------------------------------------------------------------------
# Directory for uploaded documents (created automatically)
UPLOAD_DIR=./uploads

# Maximum file size in bytes (default: 10MB)
MAX_FILE_SIZE=10485760

# -----------------------------------------------------------------------------
# EMAIL CONFIGURATION (Optional)
# -----------------------------------------------------------------------------
# SMTP settings for sending email notifications
# Leave commented out if not using email features

# SMTP server hostname
SMTP_HOST=smtp.gmail.com

# SMTP server port (587 for TLS, 465 for SSL)
SMTP_PORT=587

# SMTP authentication credentials
SMTP_USER=your_email@gmail.com
SMTP_PASS=your_app_password

# From address for emails
SMTP_FROM=your_email@gmail.com

# -----------------------------------------------------------------------------
# APPLICATION URLS
# -----------------------------------------------------------------------------
# URLs for integration with main application

# Next.js frontend URL (for CORS and redirects)
FRONTEND_URL=http://localhost:3000

# Worker API base URL (this service)
API_BASE_URL=http://localhost:8001

# =============================================================================
# PRODUCTION NOTES
# =============================================================================
# 1. Change all passwords and credentials
# 2. Use strong database passwords
# 3. Configure firewall rules for ports 8001, 5434, 11434
# 4. Enable HTTPS in production (use reverse proxy)
# 5. Set appropriate WORKER_CONCURRENCY based on server resources
# 6. Monitor disk space in UPLOAD_DIR
# =============================================================================

